import { FiltroProductoCategoriaPipe } from './filtro-producto-categoria.pipe';

describe('FiltroProductoCategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroProductoCategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
