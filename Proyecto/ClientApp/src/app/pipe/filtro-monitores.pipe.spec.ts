import { FiltroMonitoresPipe } from './filtro-monitores.pipe';

describe('FiltroMonitoresPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroMonitoresPipe();
    expect(pipe).toBeTruthy();
  });
});
